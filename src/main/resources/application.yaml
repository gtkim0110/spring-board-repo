
debug: false
management:
  endpoints:
    web:
      exposure:
        include: "*"
  server:
    port: 8080

logging:
  level:
    com.example.demo.Demo1Application: debug
    org.springframework.web.servlet: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/board
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect  # ??????? dialect ?? db ? ??. mysql ???? ?????.
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update  #entity ? ?? table ???? ???? ???? ddl ?? ????.
#    open-in-view: false
    show-sql: true # sql ?? ????
    properties:
      hibernate.format_sql: true   # ??? ??? debug? ????
      hibernate.hibernate.default_batch_fetch_size: 100 # ?? ????? ??? ? ??? select ??

  h2:
    console:
      enabled: false

  data.rest:
    base-path: /api
    detection-strategy: annotated


#spring:
#  config:
#    activate:
#      on-profile: testdb
#  datasource:

